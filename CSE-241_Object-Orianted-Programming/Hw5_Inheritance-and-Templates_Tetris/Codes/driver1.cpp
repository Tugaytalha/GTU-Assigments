#include "Tetris.h"
 

/* in this case we can use also "using namespace Tetris;" 
 * instead of writing Tetris:: again and again 
 */ 
int main() {
    test_tetris(1);
    test_tetris(2);
    test_tetris(3);
    test_tetris(4);

    return 0;
}